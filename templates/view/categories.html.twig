{% extends 'base.html.twig' %}

{% block content %}
    <div class="jumbotron">
        {% for category in categories %}
            <h3>{{category.name}}</h3>
            <ul>
                {% for childCategory in category.getChildren() %}
                    <li>
                        <a href="/?category_id={{ childCategory.id }}">{{ childCategory.name }}</a>
                    </li>
                {% endfor %}
            </ul>
        {% endfor %}
    </div>
{% endblock %}
